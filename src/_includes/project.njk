---
layout: page
---
<article class="p-8 my-16 glass-bg shadow-xl rounded-lg">
<h1 class=" text-2xl sm:text-4xl md:text-5xl font-black pt-10 md:pt-30 pb-4 leading-tight text-center">
    {{ title}}
</h1>

<div class="mt-3 pb-10 flex justify-center gap-x-3 gap-y-2 flex-wrap">
  {% for tag in tags %}
    <span class="post-tag project-tag">{{ tag }}</span>
  {% endfor %}
  </div>

<div class="prose prose-arctic dark:prose prose-arctic-invert mb-16">
  {{ content | safe }}
</div>
</article>

{% include "author.njk" %}
